import { SINGLE_ACTION_COLUMN_WIDTH } from "../../../../utils/constants";

export const columnsWidth = {
    number: 180,
    year: 180,
    actions: SINGLE_ACTION_COLUMN_WIDTH * 4,

    // upsert input operation line table
    article: 200,
    description: 150
}